---
title: UI Components
description: Browse 40+ production-ready React components with smooth animations. Buttons, inputs, modals, cards, and more. Built with TypeScript, Framer Motion, and Tailwind CSS. Fully accessible and customizable.
---

## Basic Information About Components

Install components using the CLI:

### Add Components

<PackageManagerTabs
  pnpm="pnpm dlx smoothui-cli@latest add animated-input"
  npm="npx smoothui-cli@latest add animated-input"
  yarn="yarn dlx smoothui-cli@latest add animated-input"
  bun="bunx smoothui-cli@latest add animated-input"
/>

### Add Multiple Components

<PackageManagerTabs
  pnpm="pnpm dlx smoothui-cli@latest add animated-input siri-orb grid-loader"
  npm="npx smoothui-cli@latest add animated-input siri-orb grid-loader"
  yarn="yarn dlx smoothui-cli@latest add animated-input siri-orb grid-loader"
  bun="bunx smoothui-cli@latest add animated-input siri-orb grid-loader"
/>

### Interactive Mode

<PackageManagerTabs
  pnpm="pnpm dlx smoothui-cli@latest add"
  npm="npx smoothui-cli@latest add"
  yarn="yarn dlx smoothui-cli@latest add"
  bun="bunx smoothui-cli@latest add"
/>

### Using shadcn CLI

You can also install components using the shadcn CLI:

<PackageManagerTabs
  pnpm="pnpm dlx shadcn@latest add @smoothui/animated-input"
  npm="npx shadcn@latest add @smoothui/animated-input"
  yarn="yarn dlx shadcn@latest add @smoothui/animated-input"
  bun="bunx shadcn@latest add @smoothui/animated-input"
/>

## Usage

```tsx
import { AnimatedInput, NumberFlow, WaveText } from "@repo/smoothui/components";

export function MyComponent() {
  return (
    <div>
      <AnimatedInput label="Email" placeholder="Enter your email" />
      <NumberFlow value={1234} />
      <WaveText>Hello World</WaveText>
    </div>
  );
}
```

## Customization

All components are customizable via:
- Tailwind CSS utility classes
- CSS variables for theming
- Component props for behavior and styling

Components are tree-shakeable, TypeScript-first, and accessible by default.

<Cards>
  <Card title="Browse All Components" href="/components" description="View all 40+ available components" />
  <Card title="Form Components" href="/components?category=form" description="Inputs, selects, and form elements" />
  <Card title="Animated Components" href="/components?category=animated" description="Components with smooth animations" />
  <Card title="Installation Guide" href="/docs/installation" description="Get started with SmoothUI" />
  <Card title="Theming Guide" href="/docs/theming" description="Customize colors and styles" />
  <Card title="API Reference" href="/docs/api" description="Complete component API documentation" />
</Cards>
