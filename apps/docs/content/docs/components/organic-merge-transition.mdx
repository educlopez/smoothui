---
title: Organic Merge Transition
description: A soft-min SDF merge transition with two organic expanding shapes.
icon: Merge
dependencies:
  - motion.dev
installer: organic-merge-transition
---

## Features

- Persistent WebGL transition wrapper triggered by a `transitionKey`
- Replicates the “Organic Merge” stage from the Codrops shader progression
- Uses the shared SmoothUI SDF transition engine with reduced-motion fallback
- Accessible: semantic content stays in the DOM and `onRest` still fires

## Usage guidance

Use it for meaningful route/state transitions where this shader grammar fits the product moment.

## Props

<AutoTypeTable
  path="../../packages/smoothui/components/organic-merge-transition/index.tsx"
  name="OrganicMergeTransitionProps"
/>
