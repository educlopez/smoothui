---
title: Radial Circles Transition
description: A radial SDF circle pattern transition inspired by the Codrops radial circles step.
icon: Orbit
dependencies:
  - motion.dev
installer: radial-circles-transition
---

## Features

- Persistent WebGL transition wrapper triggered by a `transitionKey`
- Replicates the “Radial Circles” stage from the Codrops shader progression
- Uses the shared SmoothUI SDF transition engine with reduced-motion fallback
- Accessible: semantic content stays in the DOM and `onRest` still fires

## Usage guidance

Use it for meaningful route/state transitions where this shader grammar fits the product moment.

## Props

<AutoTypeTable
  path="../../packages/smoothui/components/radial-circles-transition/index.tsx"
  name="RadialCirclesTransitionProps"
/>
