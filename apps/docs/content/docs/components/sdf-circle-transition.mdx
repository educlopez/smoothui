---
title: SDF Circle Transition
description: A clean SDF circle reveal based on the first Codrops shader step.
icon: Circle
dependencies:
  - motion.dev
installer: sdf-circle-transition
---

## Features

- Persistent WebGL transition wrapper triggered by a `transitionKey`
- Replicates the “SDF Circle” stage from the Codrops shader progression
- Uses the shared SmoothUI SDF transition engine with reduced-motion fallback
- Accessible: semantic content stays in the DOM and `onRest` still fires

## Usage guidance

Use it for meaningful route/state transitions where this shader grammar fits the product moment.

## Props

<AutoTypeTable
  path="../../packages/smoothui/components/sdf-circle-transition/index.tsx"
  name="SdfCircleTransitionProps"
/>
