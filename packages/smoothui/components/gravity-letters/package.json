{
  "name": "@repo/gravity-letters",
  "description": "A GravityLetters component for SmoothUI.",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "@repo/shadcn-ui": "workspace:*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "motion": "^12.23.25"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.1",
    "typescript": "^5.9.3"
  },
  "smoothui": {
    "category": "text",
    "tags": [
      "text",
      "gravity",
      "physics",
      "letters",
      "playful",
      "interactive",
      "animated"
    ],
    "complexity": "advanced",
    "animationType": "spring",
    "useCases": [
      "Playful hero headline that collapses into a pile of letters on hover",
      "Click-to-topple heading for a 404 or empty state",
      "Scroll-triggered text reveal that falls into place when it enters the viewport"
    ],
    "compositionHints": [
      "Use as a standalone headline, sized via className with a font-size utility",
      "Give the container enough height via className (e.g. h-40) so letters have room to fall"
    ],
    "hasReducedMotion": true
  }
}
